# @lijuhong1981/jstask

> 任务调度工具集，提供 `TaskPool` 任务池、`defer` 延迟对象与 `AnimationFrameUpdater` 动画帧更新器。

## 安装

```bash
npm install @lijuhong1981/jstask
```

## 导出

- `TaskPool` — 任务池，可并发执行与管理一批任务
- `defer()` — 创建延迟对象（含 `promise`/`resolve`/`reject`/`state`/`then` 等）
- `AnimationFrameUpdater` — 动画帧循环更新器
